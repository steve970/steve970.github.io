box: wercker/ruby
build:
  steps:
    # Execute the bundle install step, a step provided by wercker
    - bundle install
    # Execute a custom script step.
    - script:
      name: middleman deploy
      code: middleman build
deploy:
  steps:
    - steve970/steve970.github.io:
        token: $GH_TOKEN
        basedir: master
